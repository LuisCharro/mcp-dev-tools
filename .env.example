# Copy to .env and customize (project defaults)
# cp .env.example .env
# For machine/user-specific overrides, create .env.local (not committed)

# Core server ports
PORT=3333
SEARCH_PORT=3334
GIT_PORT=3335

# Repository configuration
REPO_ROOT=/absolute/path/to/your/repository

# Security configuration
SAFE_WRITE=false
ALLOW_GLOBS=**/*
DENY_GLOBS=**/.env*,**/*.pem,**/id_*,**/.git/**,**/.ssh/**,**/secrets/**

# File operation limits
MAX_FILE_SIZE=1048576
MAX_SEARCH_RESULTS=1000

# MCP reference servers path
# Defaults to "$HOME/mcpServers/mcp-reference-servers" if unset
MCP_REF_DIR=$HOME/mcpServers/mcp-reference-servers

# Debug and logging
DEBUG=false
LOG_LEVEL=info
