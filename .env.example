# Copy to .env and customize (project defaults)
# cp .env.example .env
# For machine/user-specific overrides, create .env.local (not committed)

# Filesystem server (supergateway -> run-filesystem.sh)
PORT=3333
REPO_ROOT=/absolute/path/to/your/repository

# Search server
SEARCH_PORT=3334

# Optional: path to local MCP reference servers (filesystem)
# Defaults to "$HOME/mcpServers/mcp-reference-servers" if unset
# Uncomment to override
# MCP_REF_DIR=/absolute/path/to/mcp-reference-servers
